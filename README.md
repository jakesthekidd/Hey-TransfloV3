
  # AI Voice agent

  This is a code bundle for AI Voice agent. The original project is available at https://www.figma.com/design/zCZGj9YX1Ho3OgUvylAdPt/AI-Voice-agent.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  